<!-- <div class="Statistics">
  <h1>Statistics</h1>
  <div class="average_age">
    <h2><%= @comedians.average_age %> minutes </h2>
  </div>
  <div class="">

  </div>
</div> -->

<% @comedians.each do |comedian| %>

<div class="individual_comedian">
  <div class="comedian_attr">
    <ul>
      <li><%= comedian.name %></li>
      <li><%= comedian.age %></li>
      <li><%= comedian.city %></li>
      <% comedian.specials.each do |special| %>
      <div class="special_attr">
        <ul>
          <li><%= special.name %></li>
          <li><%=  special.year %> </li>
          <li> <img src="<%= special.image_url %>" alt="#{<%= special.name %>}"> </li>
          <li><%= special.run_time_length %> minutes</li>
        </ul>
      </div>
      <% end %>
    </ul>
  </div>
</div>
<% end %>
